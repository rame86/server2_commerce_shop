package com.example.shop.model;


public class enums {

    public enum SellerType { ARTIST, USER }
    public enum ProductCategory { OFFICIAL, UNOFFICIAL, SECONDHAND }
    public enum OrderStatus { PENDING, PAID, SHIPPED, COMPLETED, CANCELLED }
}
